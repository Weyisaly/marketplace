import 'package:flutter/material.dart';

DialogTheme buildDialogThemeLight(ThemeData base) => DialogTheme(
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
);

DialogTheme buildDialogThemeDark(ThemeData base) => DialogTheme(
  shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(16)),
);


